# CLAUDE.md

Contesto del progetto per Claude Code. La lingua di lavoro è l'italiano (UI, commenti, messaggi).

## Cos'è

Nonostante il nome della cartella (`pdfReader_C`), il progetto attivo è **ForgeCAD**: un CAD parametrico 2D/3D in C++17 + Qt6 + OpenCASCADE (geometria esatta) + OpenGL legacy per il display (pipeline fissa, `glBegin/glEnd`), con un backend CUDA opzionale (al momento solo una sonda di disponibilità). L'interfaccia richiama "Part Studio" in stile Onshape: albero modello, piani di riferimento, schizzi, estrusioni e operazioni booleane.

I file `pdfReader.c` e `pdfView.c` sono **legacy**: esperimenti in C con MuPDF (estrazione testo) e GTK3 (visualizzatore PDF). Non sono in CMake e non fanno parte di ForgeCAD. Anche `pdfView_qt6` (binario nella root) è un vecchio artefatto.

## Requisito di precisione (vincolante)

ForgeCAD è destinato a uso ingegneristico: **tutta la geometria è esatta**. Il kernel è **OpenCASCADE 7.9** (pacchetto Arch `opencascade`, include in `/usr/include/opencascade`, LGPL-2.1 con eccezione).
- Coordinate del modello in `double` (`QPointF` per lo schizzo, `gp_Pnt`/`TopoDS_Shape` in 3D). `float`/`QVector3D` solo per il disegno a schermo.
- Curve di schizzo memorizzate come parametri esatti e convertite in `Geom2d_*`; solidi come B-rep (`TopoDS_Shape`); booleane con `BRepAlgoAPI_*`.
- La tassellazione (`BodyDisplay`, `CurveObject::samples`) serve solo a disegnare e a selezionare a video: **non va mai usata per calcoli** (volumi, booleane, misure). Niente CSG su mesh o geometria fatta in casa.
- Tolleranze: `Precision::Confusion()` (1e-7) nel kernel, `ForgeCad::kSketchConnectionTolerance` (1e-6) per unire gli estremi dello schizzo nei contorni.
- Eccezione concordata (settembre 2026): in `kernel/` si sviluppa un kernel geometrico proprio (vedi sotto). Resta **affiancato** a OCCT, che rimane il kernel di produzione e fa da riferimento nei test. Una funzione dell'app passa al nuovo kernel solo quando i test contro OCCT la confermano. Da settembre 2026 estrusioni, rivoluzioni, primitive, booleane, raccordi e smussi si possono costruire con l'uno o con l'altro: *Opzioni → Kernel geometrico* (OpenCASCADE di default; la scelta resta in `QSettings`, chiave `kernel/geometry`).

## Build ed esecuzione

```fish
cmake -S . -B forgecad-cuda-build -DCMAKE_BUILD_TYPE=Debug   # solo la prima volta
cmake --build forgecad-cuda-build -j
./forgecad-cuda-build/forgecad
```

- Directory di build esistente: `forgecad-cuda-build/` (Debug, `CMAKE_CUDA_ARCHITECTURES=native` → sm_75 per la Quadro RTX 3000, nvcc 13.4, Qt6 trovato in `/opt/cuda/lib/cmake/Qt6`).
- La build compila pulita (a settembre 2026).
- CUDA: se esiste `/opt/cuda/bin/nvcc` si compila `cuda_support.cu` e si definisce `FORGECAD_HAS_CUDA=1`; altrimenti si usa il fallback `cuda_support_cpu.cpp` (`FORGECAD_HAS_CUDA=0`).
- GPU ibrida (Intel UHD P630 + NVIDIA): l'ambiente di sessione forza EGL su Mesa (`__EGL_VENDOR_LIBRARY_FILENAMES=50_mesa.json`), quindi Qt su Wayland renderizzerebbe su Intel. `preferDiscreteGpu()` in `forgeCad2026.cpp` imposta le variabili PRIME/EGL NVIDIA prima di `QApplication`; `FORGECAD_IGPU=1` la disattiva. La status bar mostra il `GL_RENDERER` reale.
- Il viewport è a pipeline fissa: `requestCompatibilityContext()` (sempre in `forgeCad2026.cpp`) chiede un contesto desktop OpenGL in compatibility profile con depth 24. Senza, NVIDIA via EGL dà un contesto **OpenGL ES** e `glBegin`/luci non disegnano nulla (restano visibili solo gli overlay QPainter).
- Warning attivi: `-Wall -Wextra -Wpedantic` (solo CXX).
- `compile_commands.json` è un symlink verso la build; `.clangd` rimuove `-mno-direct-extern-access`.
- Il progetto è in un repository git (anche la directory di build è tracciata). L'app si verifica compilando e avviandola; il kernel proprio ha test automatici: `./forgecad-cuda-build/kernel/forgekernel_tests [filtro]` oppure `ctest` nella directory di build.

## Mappa dei file

| File | Ruolo |
|---|---|
| `forgeCad2026.cpp` | `main()`: crea `QApplication` e `PdfWindow`. |
| `forgeCad2026_gui.h/.cpp` | **Il grosso del codice (~1600 righe).** `CadViewport` (un `QOpenGLWidget`, definito e implementato tutto inline nel .cpp) e `PdfWindow` (un `QMainWindow`: menu, toolbar, dock con l'albero modello, status bar, tema scuro). |
| `cad_types.h` | Tipi del modello (double): `CurveObject` (parametri esatti + `samples` di display + `construction`), `SketchObject` (con `constructionSegments`: indici dei segmenti di costruzione; `plane` 0/1/2 o `kFacePlane` = 3 con `frame` (`SketchFrame`: origine, asse X e normale uscente esatti) e `faceSource`), `FaceHit` (faccia sotto il puntatore: indice, piano se piana, punti medi esatti dei suoi spigoli senza cuciture), `ExtrusionObject` (corpo parametrico: `feature` estrusione/rivoluzione/primitiva o booleana, + `TopoDS_Shape shape` + `BodyDisplay display` + `error`), `PrimitiveParameters`, `BooleanOperation`, `DocumentState`, `BackgroundSettings`. |
| `cad_curve_solver.h/.cpp` | `curveGeometry(curve)` → `Geom2d_Curve` esatte: spline = Bezier cubiche C1 a tratti come B-spline grado 3 (nodi interni molteplicità 3), NURBS (poli + `weights`, nodi uniformi clamped, grado ≤3), cerchio, arco (centro/inizio/fine), poligono (lati come segmenti). `recalculateCurve` produce solo i `samples` di visualizzazione (`GCPnts_TangentialDeflection`). |
| `cad_history.h/.cpp` | `ForgeCad::History`: Undo/Redo a istantanee complete di `DocumentState` (schizzi + estrusioni), limite 200. |
| `cad_kernel.h/.cpp` | Modellazione esatta con OpenCASCADE: `sketchAxes`/`sketchToWorld`/`worldToSketch`/`extrusionVector` (per indice di piano o per schizzo: gli schizzi su faccia usano `frame`, l'estrusione positiva va lungo la normale uscente), `faceSketchFrame` (Y dello schizzo = Z del modello proiettato sul piano, Y se il piano e' orizzontale; X = Y × normale; origine = proiezione dell'origine del modello, cosi' le coordinate di vista coincidono con quelle dello schizzo), `pickFace` (`IntCurvesFace_ShapeIntersector`, faccia piu' vicina, piano con il verso della faccia), `buildSketchProfile` (contorni chiusi → facce con fori annidati, altrimenti fili aperti), `buildExtrusion` (`BRepPrimAPI_MakePrism` + `BRepCheck_Analyzer`), `buildRevolution` (`BRepPrimAPI_MakeRevol`; asse da `sketchRevolutionAxis`, lato del profilo da `revolutionProfileSide`), `buildPrimitive` (`BRepPrimAPI_Make{Box,Cylinder,Sphere,Cone,Torus}` nel sistema `primitiveAxes`), `buildBlend` (`BRepFilletAPI_MakeFillet`/`MakeChamfer` sugli spigoli piu' vicini ai punti dati), `booleanOperation` (Fuse/Common/Cut + `ShapeUpgrade_UnifySameDomain`), `tessellate` (solo display, spigoli B-rep senza cuciture), `intersectRay` (selezione sulla forma esatta). |
| `cad_export.h/.cpp` | Esportazione per altri CAD (*File → Esporta*): `exportBodies` scrive i corpi visibili (`CadViewport::exportableBodies`; col kernel proprio le forme OCCT si rigenerano dalla definizione e il documento torna com'era) in STEP AP242/AP214/AP203 (`STEPCAFControl_Writer`, schema da `write.step.schema` impostato prima dello scrittore) o IGES 5.3 solidi 186 / superfici 144 (`IGESCAFControl_Writer`, `write.iges.brep.mode`). Documento XCAF in mm, un prodotto per corpo con nome (ASCII in IGES) e colore (non in AP203). Parasolid (x_t) non c'e': OCCT non lo scrive (formato proprietario Siemens). |
| `cad_document_io.h/.cpp` | File `.prt`: `saveDocumentFile` / `loadDocumentFile`. Solo la definizione parametrica (schizzi con i parametri esatti, funzioni con i parametri; niente B-rep ne' tassellazioni, che si rigenerano all'apertura con il kernel attivo). Formato: `FCAD`, versione quint16 (2: aggiunge `frame` e `faceSource` degli schizzi; 3: i vincoli oggetto `geometricConstraints`; 4: posizione delle quote, `customFrame` degli schizzi e orientamento degli assi; le versioni vecchie si leggono ancora e i loro vincoli si convertono), compressione quint8 (1 = zlib), poi `qCompress(..., 9)` del contenuto `QDataStream` (Qt_6_0, double a 64 bit, valori esatti). Salvataggio atomico con `QSaveFile`; in lettura controlli di versione, di lunghezza e degli array paralleli dei segmenti. Se si aggiungono campi al modello: nuova versione del formato, con la lettura delle vecchie. |
| `cad_sketch_edit.h/.cpp` | Modifica delle entita' dello schizzo (su una copia, lo schizzo cambia solo se riesce): `trimSketchEntity` / `trimPreview`, `extendSketchEntity`, `splitSketchEntity`, `blendSketchSegments` (raccordo = arco tangente, smusso = segmento), `sketchCornerAt`, `removeSketchEntities` (compattazione degli array paralleli e dei vincoli, mappa dei segmenti). Intersezioni con `Geom2dAPI_InterCurveCurve` sulle curve di `curveGeometry`; i tratti di segmenti/archi/cerchi restano segmenti/archi esatti, quelli di spline sono tratti di Bezier esatti (de Casteljau); le NURBS fanno solo da bordo; i poligoni si scompongono nei lati. Vincoli di coincidenza delle entita' toccate aggiornati (`refreshCoincidences`). |
| `cad_snap.h/.cpp` | `ForgeCad::snapSegments` in double: punti notevoli (estremi delle curve, centri, vertici, origine del piano se `originSnap_`), estremi e punti medi dei segmenti; solo se nessun punto e' entro la tolleranza il punto più vicino di un segmento; poi griglia (passo 0.25). *Schizzo → Snap all'origine* (`sketch/originSnap`, etichetta "✚ Origine"). Nel viewport `snapPoint` aggiunge le curve: quadranti esatti di cerchi e archi (come punti, "◆ Quadrante") e, se piu' vicino del punto su un segmento, il punto esatto piu' vicino sulla curva (`nearestCurvePoint`: cerchi/archi in forma chiusa, altre curve con `Geom2dAPI_ProjectPointOnCurve`; "∈ Su curva", `lastSnapCurve_`). In `inferLinePoint` una direzione (H/V/⟂/∥) su un cerchio o arco prende il punto comune esatto retta-cerchio; su una spline il punto resta quello agganciato. |
| `cad_constraints.h/.cpp` | Vincoli geometrici come oggetti (`SketchConstraint` in `SketchObject::geometricConstraints`: tipo, uno o due `ConstraintRef` — segmento, curva, un loro punto, o origine/assi del piano con `kind` 2 — valore delle quote, posizioni del vincolo fisso). Tipi: coincidente, orizzontale, verticale, parallelo, perpendicolare, collineare, tangente, uguale, concentrico, punto medio, punto sull'entita', fisso, distanza, angolo, raggio, diametro (`applicableConstraints` dice quali valgono per i riferimenti scelti; `makeConstraint` prende la misura attuale). Tangenza: se le due entita' hanno un punto in comune (vincolo di coincidenza diretto tra estremi di segmenti o archi, o il punto del raggio di un cerchio: `tangentContact`) si scrive nel punto (retta perpendicolare al raggio, centri allineati con il punto); la forma a distanze insieme alla coincidenza nel contatto e' degenere al primo ordine (gradi di liberta' contati in piu', convergenza lenta). `solveSketch`: Gauss-Newton a passo di norma minima (J^T (J J^T + mu I)^-1 r, Jacobiano per differenze centrali, Cholesky, ricerca lineare) sulle coordinate di tutti i punti: i punti liberi non si muovono, gli altri il meno possibile; equazioni implicite (fine dell'arco sul suo cerchio, semiassi dell'ellisse perpendicolari); bersagli (`PointTarget`) per il trascinamento e le quote; soluzione a 1e-13 della dimensione dello schizzo, oltre 1e-9 conflitto (lo schizzo resta com'era). `migrateLegacyConstraints`: i vecchi codici H/V, le lunghezze e gli angoli quotati e le coincidenze diventano oggetti (all'apertura dei file prima della versione 3). `remapConstraints` dopo l'eliminazione di entita'; `constraintAnchors` per i simboli nella vista; `dimensionPoints`/`constraintLines`/`circleOf` per disegnare le quote. `analyzeSketch`: gradi di liberta' = coordinate meno il rango del Jacobiano di vincoli, equazioni implicite e "gauge" (il punto del raggio di un cerchio libero di girare, se nessun vincolo lo usa), rango con Gram-Schmidt modificato a due passate; una coordinata e' determinata se il suo versore sta nello spazio delle righe (entita' completamente definite). |
| `cad_icons.h/.cpp` | `commandIcon(nome)`: icone dei comandi disegnate con QPainter (`QIconEngine` proprio, vettoriali: nitide a ogni dimensione e rapporto dei pixel, niente file) su una griglia 24 x 24, colori per il tema scuro (tratto, accento arancio per l'entita' che il comando crea, azzurro tratteggiato per la costruzione, rosso per quello che si toglie; grigie se disattivate). `commandIconNames()` le elenca. |
| `cuda_support.*` | `forgecad_cuda_backend()` / `forgecad_cuda_available()`. |
| `cad_forge.h/.cpp` | Modellazione con il kernel proprio, alternativa a `cad_kernel`: `forgeSketchSegments` (curve dello schizzo nel nuovo kernel, stessa geometria di `curveGeometry`), `forgeSketchFrame`, `forgeExtrusion` (regioni chiuse → solido; se non ce ne sono, catene aperte → lamina), `forgeRevolution` (lo schizzo portato con un movimento rigido nel semipiano del profilo, poi `makeRevolution`), `forgePrimitive`, `forgeBlend` (spigoli dai punti con `nearestEdge`, poi `blendEdges`), `forgeBoolean`, `forgeTessellate` (display, stessa deflessione di `tessellate`), `forgeIntersectRay` (selezione sulla geometria esatta), `forgePickFace` (`firstRayHit` con la faccia). |
| `cad_kernel_lab.h/.cpp` | Banco di prova del menu *Debug → Kernel sperimentale*. Costruisce una superficie (sfera, toro, cono, NURBS, rivoluzione, estrusione) sia con il nuovo kernel sia con OCCT e proietta il punto sotto il mouse con entrambi, confrontando risultato e tempi. Mostra anche due solidi B-rep del nuovo kernel (prisma con fori, cilindro) con conteggi, genere ed esito di `checkBody`. *Debug → Confronta il corpo selezionato col nuovo kernel* converte lo schizzo del corpo selezionato (`forgeSketchSegments`), lo estrude con `makeExtrusion` e lo confronta con la forma OCCT del documento (conteggi, SP-curve esatte, volume, area, baricentro, lunghezza degli spigoli). Se il corpo è una booleana lo ricostruisce ricorsivamente con `booleanOperation` e confronta volume, area, baricentro e numero di facce con la forma OCCT. Non tocca il documento. |
| `kernel/` | Kernel geometrico proprio in sviluppo (libreria `forgekernel` + `forgekernel_tests`). Vedi la sezione dedicata. |

## Architettura di `CadViewport`

- **Stato**: tutto in membri privati alla fine della classe: `sketches_`, `extrusions_`, camera (`yaw_`, `pitch_`, `zoom_`), modalità (`sketchMode_`, `drawingTool_`, `constraintMode_`, `displayMode_`, `lightingPreset_`, `tessellationQuality_`), selezioni (`selection_`, `sketchSelections_`, `selectedPoints_`), drag dei punti di controllo e curva in costruzione (`curveControlPoints_`, `pendingPoint_`, `hasPendingPoint_`).
- **Rendering** (`paintGL`): proiezione ortografica → sfondo sfumato → piani di riferimento → griglia → luci → estrusioni → schizzo → marker di snap → etichette (QPainter) → evidenziazione di hover e selezione (QPainter). I piani sono trasparenti con un bordo del loro colore schiarito (alone largo e tenue + linea sottile, piu' intensi con hover e selezione). Alla fine della parte OpenGL `makeOpaque()` riporta a 1 il canale alfa del framebuffer: la fusione `GL_SRC_ALPHA`/`GL_ONE_MINUS_SRC_ALPHA` lo abbassa dove c'e' qualcosa di trasparente e il compositore Wayland (niri) mostrerebbe il desktop attraverso la finestra. Chi aggiunge disegni trasparenti non deve preoccuparsene, ma non va tolto.
- **Sfondo** (`background_`, `BackgroundSettings`): sfumatura a due colori con angolo e posizione del punto di mescolanza, disegnata a bande in coordinate schermo. Se `affectsLighting` è attivo, `configureEnvironmentLighting` usa `GL_LIGHT2`/`GL_LIGHT3` (direzionali in coordinate vista, dai lati dei due colori) e tinge la luce ambiente; le luci principali (`GL_LIGHT0`/`GL_LIGHT1`) vengono attenuate di conseguenza. Si modifica da *Visualizza → Sfondo e luce ambiente...* (`PdfWindow::editBackground`, con anteprima dal vivo).
- **Hover e selezione**: `SceneSelection` con `SceneObjectKind { None, Plane, Sketch, Extrusion }`; `hover_` (azzurro, `kHoverColor`) e `selection_` (giallo, `kSelectionColor`). `pickSceneObject` ha questa priorità: linee degli schizzi (in pixel) → estrusioni (raycast con `viewRay` + `intersectExtrusion`, vince la più vicina) → piani (`pickReferencePlane`). Le estrusioni sotto il puntatore si illuminano (colore + `GL_EMISSION`) e hanno un contorno fatto con lo stencil (`drawExtrusionOutline`); piani e schizzi sono evidenziati con QPainter (`strokeHighlight`). In modalità schizzo l'hover è `sketchHover_` sugli elementi dello schizzo attivo.
- **Undo/Redo**: ogni modifica al documento segue lo schema `recordUndo();` → modifica → `documentChanged();`. `documentChanged` notifica la finestra, che ricostruisce l'albero (`rebuildModelTree`) e aggiorna Annulla/Ripeti. Il trascinamento dei punti di controllo registra l'istantanea al primo movimento effettivo (`dragSnapshot_`). `restoreDocument` azzera lo stato transitorio (curve in costruzione, selezioni) ed esce dalla modalità schizzo se lo schizzo attivo non esiste più. Anche la visibilità degli oggetti è annullabile; la visibilità dei piani no.
- **Visibilità**: `setObjectVisible` / `showAllObjects`. Gli oggetti nascosti non vengono disegnati né selezionati (lo schizzo attivo in modalità schizzo viene comunque disegnato). I piani hanno `planeVisible_[3]` oltre al flag globale `referencePlanesVisible_`.
- **Coordinate**: i punti di schizzo sono `QPointF` (double) nel piano. `ForgeCad::sketchAxes(plane)` dà il `gp_Ax3` del piano (`0 = XY` normale +Z, `1 = XZ` normale −Y con assi X,Z, `2 = YZ` normale −X con assi Z,Y); `mapSketchPoint` nel viewport converte solo per il display. `extrusionVector` estrude lungo +Z / +Y / +X per distanza positiva.
- **Modalità schizzo**: `beginSketchMode(plane)` blocca la camera sulla vista normale al piano e attiva lo strumento *Selezione* (`DrawingTool::Select`, anche il default di `drawingTool_`). `Esc` annulla prima il disegno in corso, poi torna alla selezione, poi esce (`endSketchMode()`). I cambi di strumento decisi dal viewport arrivano alla finestra con `setToolChangedCallback` (le azioni del gruppo degli strumenti hanno `data()` = strumento). Comandi mouse:
  - Selezione: clic seleziona un'entita', `Maiusc+clic` aggiunge o toglie, `Ctrl+clic` sceglie punti per i vincoli, clic sul simbolo di un vincolo lo seleziona, doppio clic sulla quota. Trascinamento (solo con la Selezione: con gli strumenti di disegno il clic crea sempre, anche sui punti di altre entita'): un estremo di segmento (`segmentEndpointAt`) si sposta con tutti i punti coincidenti (`dragSketchPoint` → `moveSketchPoint`, poi il risolutore dei vincoli con i punti trascinati come bersagli), agganciandosi agli altri punti, ai segmenti che non vi arrivano, all'origine e alla griglia (`dragSnapPoint`); l'interno di un segmento lo sposta intero dopo qualche pixel (`dragSketchSegment`); i punti delle curve come prima (`findCurveEditPoint`). Un passo di Undo per trascinamento; al rilascio si azzerano le quote di lunghezza non piu' vere e si rigenera. Cursore a croce sopra i punti trascinabili.
  - Linea/Polilinea: clic dopo clic. I vincoli orizzontale/verticale/automatico, la quota di lunghezza e la quota angolare vengono applicati al segmento. In `SketchObject` si scrivono insieme `segments`, `constraints`, `segmentLengths` e `segmentAngles`: sono **array paralleli** e vanno tenuti allineati.
  - Spline/NURBS: clic per aggiungere punti; `Invio` o tasto destro per chiudere (`finalizeCurve`, le NURBS richiedono almeno 4 punti). `Shift+clic` aggiunge un punto a una curva esistente; trascinando si muovono i punti di controllo e le maniglie tangenti.
  - Rettangolo (2 clic: angoli opposti) e Rettangolo dal centro (centro e angolo), con Maiusc quadrato (`rectangleCorner`) → `finalizeRectangle`: quattro segmenti con i vincoli orizzontale/verticale e le coincidenze (non una curva; `DrawingTool::Rectangle/CenterRectangle` esistono in `curveGeometry` solo per l'anteprima).
  - Ellisse (3 clic: centro, estremo del primo semiasse, punto la cui distanza dalla retta di quel semiasse e' il secondo; `ellipseMinorPoint`): `CurveObject` con centro e i due estremi dei semiassi (il terzo sta sempre sulla perpendicolare: `normalizeEllipse` quando si trascinano i punti, il centro trascina gli altri). Geometria esatta `Geom2d_Ellipse` / `Kernel::Ellipse<2>` con il semiasse maggiore lungo X (stesso parametro); quote con `editEllipseDimension` (semiassi, angolo, centro); aggancio al centro e ai quattro vertici; tagliare/estendere non la modificano (fa solo da bordo). I nuovi `DrawingTool` stanno in fondo all'enum (valori salvati nei file).
  - Arco per tre punti (`DrawingTool::ThreePointArc`: inizio, fine, un punto dell'arco; centro = circocentro, `threePointArc`) e Arco tangente (`TangentArc`: clic sull'estremo di un segmento o di un arco, poi la fine; `tangentStartsAt` da' le direzioni che proseguono le entita' in quel punto e `tangentStartFor` sceglie quella che va di piu' verso il cursore, per gli angoli tra due entita'; centro sulla normale a distanza |d|²/(2 n·d), `tangentArc`) → `finalizeDrawnArc`: creano un normale `Arc` (centro, inizio, fine in senso antiorario: se l'arco gira in senso orario gli estremi si scambiano), con le coincidenze degli estremi (`recordPointCoincidences`) e, per quello tangente, il vincolo Tangente con l'entita' di partenza. Anteprima: la corda, poi l'arco. Niente tangenza agli estremi delle spline (il vincolo non le accetta).
  - Cerchio/Poligono (2 clic) e Arco (3 clic: centro, inizio, fine) → `finalizePrimitive`, che salva solo i parametri esatti (punti con aggancio a griglia/geometria, `sides` per il poligono). I punti di cerchi/archi/poligoni si possono trascinare (modifica parametrica).
  - `Ctrl+clic`: sceglie o toglie un punto (anche l'origine del piano) per i vincoli.
  - Aggancio e vincoli durante il disegno delle linee (`inferLinePoint`): il punto si aggancia a estremi, punti medi e segmenti (`snapPoint`); in modalita' automatica la direzione si aggancia (entro 4 gradi) a orizzontale, verticale, perpendicolare al segmento da cui si parte (`startReference_`), parallela a un segmento esistente (a un estremo o punto medio agganciato il punto resta quello; su un segmento si prende il punto comune). Vicino al cursore compaiono le etichette del vincolo (`drawInferenceTags`: Coincidente, Punto medio, Su segmento, Orizzontale, Verticale, Perpendicolare, Parallelo, quote) e il segmento di riferimento tratteggiato. Il codice in `constraints` vale 1 H, 2 V, 3 perpendicolare, 4 parallelo (-1/0 libero/automatico). Alla conferma gli estremi agganciati diventano `CoincidentConstraint` (`recordCoincidences`). Se il secondo estremo di una *Linea* (o linea di costruzione) finisce su un punto di un'altra entita', si torna alla *Selezione*; la *Polilinea* prosegue da li'. Un clic su un estremo o punto medio di un segmento comincia un segmento collegato; sull'interno lo seleziona.
  - Quote di cerchi, archi e poligoni: doppio clic sulla curva (o `D` con la curva selezionata) → `editCurveDimension`: raggio/diametro, centro, lati del poligono; i punti coincidenti con il centro e con i punti della curva seguono (`moveSketchPoint`), i punti solo appoggiati sulla curva no.
  - Quote: doppio clic su un segmento (o *Schizzo → Modifica quota del segmento*, `D`) → lunghezza e angolo (`editSegmentDimension`). Il primo estremo resta fermo; `moveSketchPoint` sposta i punti coincidenti con il secondo e propaga ai segmenti orizzontali/verticali collegati quanto serve per restarlo (un rettangolo si allunga restando rettangolo). La lunghezza diventa quotata (`segmentLengths`).
  - Anteprima durante la creazione: `previewCurve` costruisce (solo per disegnare) la curva in costruzione con il cursore come punto successivo (cerchio e poligono dopo il centro, arco: cerchio del raggio poi arco, spline/NURBS con il punto in piu'); `drawCurvePreview` la tratteggia in arancione con il raggio. Le quote vicino al cursore (`previewMeasures`, dai parametri esatti; lunghezza delle spline con `GCPnts_AbscissaPoint`): R e diametro, R/lato/lati, R/angolo/lunghezza dell'arco, lunghezza e punti; per le linee L e A del segmento in costruzione. Le etichette restano dentro la vista.
  - Strumenti di modifica (*Schizzo → Modifica entita'*, anche nella barra dello schizzo; `DrawingTool` Trim/Extend/Split/Fillet/Chamfer): Taglia (`T`, clic sul tratto da togliere, anteprima rossa `trimPreview_`), Estendi (`E`, clic vicino all'estremo di un segmento o arco), Spezza (`S`, clic nel punto, aggancio alle intersezioni e al punto medio), Raccordo (`R`) e Smusso (`M`) tra segmenti: la misura si chiede scegliendo lo strumento; clic su uno spigolo (estremo comune a due soli segmenti) o sui due segmenti dalla parte da tenere (`blendFirst_`, `Esc` annulla). `applyEditTool` lavora su una copia e fa un passo di Undo; gli errori vanno nella barra di stato (`setStatusCallback`). La quota di lunghezza dei segmenti accorciati o allungati si azzera.
  - `Canc` in modalita' schizzo elimina le entita' selezionate (`deleteSketchElements`: array paralleli, linee di costruzione, vincoli e assi delle rivoluzioni rinumerati).
- **Vincoli** (`cad_constraints`): disegnando, i vincoli dedotti diventano oggetti (orizzontale/verticale, perpendicolare/parallelo al segmento di riferimento `LineInference::reference`, quote di lunghezza e angolo, coincidenze, punto medio o punto su un segmento o su un cerchio: `recordCoincidences`); il rettangolo ha H/V e coincidenze, il raccordo dello schizzo e' tangente ai due segmenti; spezzare un segmento copia i suoi vincoli di direzione sui pezzi; tagliare/estendere tiene i vincoli ancora soddisfatti (`refreshCoincidences`). Ogni modifica passa dal risolutore (`solveActive`): il trascinamento di punti e segmenti mette i punti trascinati come bersagli (se i vincoli non lo permettono il punto resta), le quote (`editSegmentDimension` crea o aggiorna la quota Distanza; raggio e diametro seguono la finestra del cerchio) e `sketchEdited` risolvono. Selezione per i vincoli (`constraintSelection`): le entita' (clic, Maiusc aggiunge) e i punti con Ctrl+clic (anche l'origine; il Ctrl+clic non unisce piu' i punti: la coincidenza si sceglie nella finestra). I simboli dei vincoli stanno accanto alle entita' (`constraintGlyphs`: etichette con simbolo e valore, impilate; la coincidenza e' un punto verde), clic = selezione (Maiusc aggiunge), doppio clic su una quota = valore (`editConstraintValue`, si riapre finche' il valore non va bene), Canc elimina i vincoli selezionati (prima delle entita'), Esc toglie la selezione prima di uscire. Finestra fluttuante **Vincoli** (`PdfWindow`, *Schizzo → Vincoli...*, `K`, aperta con la modalita' schizzo): i vincoli possibili per la selezione (clic = vincolo; un conflitto da' l'errore e non cambia nulla), l'elenco dei vincoli dello schizzo o solo delle entita' scelte (in arancione quelli non soddisfatti), *Valore...*, *Elimina*, *Mostra i vincoli nella vista* (`setConstraintsVisible`). Il viewport la aggiorna con `setConstraintPanelCallback` (selezione, documento, ingresso/uscita dallo schizzo).
- **Gradi di liberta' e quote**: `sketchAnalysis()` (in cache, ricalcolata a ogni modifica) da' i gradi di liberta' dello schizzo attivo, scritti in alto nella vista e nella finestra Vincoli; nello schizzo attivo le entita' completamente definite sono bianche (in SolidWorks nere), le altre con i colori di sempre. Le quote (Distance, Angle, Radius, Diameter) si disegnano come nel disegno tecnico (`dimensionGraphic`/`drawDimensions`): linea di misura parallela con linee di riferimento (stacco e oltrepasso), frecce piene (fuori se non c'e' spazio), testo allineato e leggibile; raggio con il richiamo dal centro e "R", diametro attraverso il centro con "⌀", angolo con l'arco dalla prima retta per il valore con segno e linee di riferimento lungo le rette. Posizione in `SketchConstraint::placement` (`placed`), di default fuori dalla figura (dalla parte opposta al centro dello schizzo); si trascinano con la Selezione (`dimensionDrag_`, un passo di Undo), doppio clic = valore. I vecchi "L = ..." su tutti i segmenti non ci sono piu'.
- **Orientamento degli assi** (`AxesOrientation` in `DocumentState`, salvato nel file dalla versione 4, fuori dall'Undo): le direzioni del modello a destra, in alto e verso l'osservatore nella vista frontale; di default Z in alto e Y verso il fondo. `basisMatrix()` si applica dopo la rotazione della camera (`paintGL`, `projectWorldPoint`, `viewRay`, `fitView`): le viste standard (1–6: frontale, posteriore, destra, superiore, isometrica, trimetrica), l'orbita attorno all'asse in alto e la griglia (nel piano orizzontale) la seguono. `setViewFrame` porta le direzioni del modello nello spazio della vista con la base. Gli schizzi nuovi sui piani di riferimento hanno `customFrame` con `referenceSketchFrame(plane, orientamento)`: normale verso l'osservatore della vista standard in cui il piano si vede di fronte, X a destra e Y in alto sullo schermo (con Z in alto: XY dall'alto, XZ di fronte, YZ da destra con Y a destra e Z in alto); gli schizzi vecchi tengono gli assi di `sketchAxes(int)`. La vista dello schizzo e' sempre allineata ai suoi assi (`screenToSketchPoint` lo richiede). *Opzioni → Orientamento degli assi...*: asse in alto e asse verso l'osservatore (24 combinazioni), preimpostati Z in alto / Y in alto, *La vista corrente come frontale* (`orientationFromCurrentView`, anche obliqua), *Predefinito per i documenti nuovi* (`QSettings` `view/axesOrientation`, usato da *Nuovo*, all'avvio e per i file senza orientamento). "Zoom tutto" si ripete al ridimensionamento finche' l'utente non muove la vista (`autoFit_`).
- **Entita' di costruzione**: strumento *Linea di costruzione* (come Linea, il segmento finisce in `constructionSegments`) e *Schizzo → Costruzione on/off* (`C`) sulle entita' selezionate (segmenti e curve, `CurveObject::construction`). Disegnate tratteggiate (`glLineStipple`), agganciabili, escluse dai profili (`sketchEdges`, `forgeSketchSegments`, `revolutionProfileSide`).
- **Rivoluzione** (`createRevolution`, *Funzioni → Rivoluzione...*): contorni chiusi di uno schizzo attorno a un suo segmento (le linee di costruzione proposte per prime; anche un lato del profilo) o agli assi X/Y del piano (`revolveAxis` -1/-2), angolo con segno (`revolveAngle`, 360 = giro completo). Il profilo deve stare da una parte dell'asse. Si rigenera con lo schizzo.
- **Facce**: il clic su un corpo seleziona anche la faccia colpita (`selectedFace_`, `pickBodyFace` → `pickFace`/`forgePickFace`; bordi evidenziati in giallo da `drawSelectedFace`, che trova le polilinee di display piu' vicine ai punti dei suoi spigoli con `faceDisplayEdges`; si azzera a ogni `documentChanged`). Menu contestuale della vista su una faccia: *Nuovo schizzo sulla faccia* (solo piane), *Raccordo/Smusso dei bordi della faccia...* (vanno diretti alla misura con `edgePickFinished_`). *Schizzo → Nuovo schizzo sulla faccia selezionata* (`createSketchOnSelectedFace`).
- **Schizzi su faccia** (`createFaceSketch`): `SketchObject::plane = kFacePlane` con il piano esatto della faccia al momento della creazione (non segue il corpo se questo cambia). `beginSketchMode` orienta la vista con `setViewFrame` (asse X dello schizzo a destra, normale verso l'osservatore): la camera ha anche `roll_` (rotazione Rz(roll) Rx(pitch) Ry(yaw), in `paintGL`, `projectWorldPoint`, `viewRay`, `fitView`; le viste predefinite lo azzerano, l'orbita lo raddrizza a poco a poco). I punti si mappano con `mapSketchPoint(p, sketch)` / `mapActiveSketchPoint`. Nell'albero: "[Faccia di <corpo>]".
- **Raccordi e smussi** (*Funzioni → Raccordo spigoli... / Smusso spigoli...*): col corpo selezionato si entra nella scelta degli spigoli (`beginEdgePick`: clic sulle polilinee di `BodyDisplay::edges` entro 8 pixel, `pickEdge`; lontano dagli spigoli il clic su una faccia accende o spegne tutti i suoi bordi, evidenziati al passaggio (`hoverFaceEdges_`); se era selezionata una faccia del corpo si parte dai suoi bordi; Invio conferma, Esc annulla; `drawPickedEdges`), poi la finestra chiede raggio o distanza e `createBlend` crea il corpo (feature `Blend`, `firstBody` = base, nascosta come gli operandi delle booleane). Gli spigoli sono memorizzati come punti (`EdgePoint`, un campione interno della polilinea, che sta sulla curva esatta): dopo una rigenerazione si prende lo spigolo piu' vicino. Si rigenera con la base.
- **Primitive** (`createPrimitive`, *Funzioni → Primitive*): parallelepipedo, cilindro, sfera, cono (anche tronco), toro; origine e piano di riferimento (asse Z = normale del piano). Non dipendono da schizzi.
- **Estrusione** (`createExtrusion`, restituisce l'errore o stringa vuota): profili dello schizzo attivo da `buildSketchProfile`; più contorni chiusi e fori annidati sono supportati; un profilo aperto dà una superficie (`solid = false`).
- **Assi e griglia**: `drawAxes` disegna gli assi X (rosso), Y (verde), Z (blu) dall'origine con una freccia conica e la lettera in punta (QPainter in `drawReferenceLabels`), sotto gli oggetti. Lunghezza fissa in unita' del modello (`axisLength_`, default `kDefaultAxisLength` = 2, meta' del lato dei piani di riferimento da -4 a 4), sottomenu *Visualizza → Assi*: *Mostra gli assi* (`axesVisible_`, `view/axes`), *Sempre in primo piano* (`axesOnTop_`, `view/axesOnTop`, default si': `drawAxes` dopo schizzi e marcatori, senza test di profondita'; altrimenti subito dopo la griglia, sotto gli oggetti), *Dimensione degli assi...* (`view/axisLength`, 0.1–100): seguono lo zoom come il resto della scena e non dipendono dagli oggetti. *Visualizza → Griglia* la spegne (`QSettings` `view/grid`).
- **Antialiasing** (*Visualizza → Antialiasing*: spento, 2x–16x MSAA; `QSettings` `view/antialiasing`, default 4x, limitato a `GL_MAX_SAMPLES`): la parte OpenGL di `paintGL` va in un `QOpenGLFramebufferObject` multisample (con depth+stencil per il contorno), risolto in uno normale e copiato nel framebuffer del widget; le sovrapposizioni QPainter vengono dopo. Il formato del `QOpenGLWidget` non si puo' cambiare dopo la creazione, per questo il framebuffer e' nostro.
- **Zoom e inquadratura**: `zoom_` e' l'altezza visibile in unita' del modello (mezza altezza `zoom_ / 2`). I limiti dipendono dalla geometria visibile (`sceneGeometryPoints`: spigoli/triangoli dei corpi e schizzi, i piani se la scena e' vuota; box in cache `sceneMin_`/`sceneMax_`, invalidato da `documentChanged` e dall'ingresso/uscita dallo schizzo): `zoomLimits` = da 1/5000 a 4 volte lo zoom che inquadra la sfera attorno al box; `setZoom` li applica (rotella e +/- moltiplicativi). *Visualizza → Zoom tutto* (`0`, `resetZoom` → `fitAll`) inquadra tutta la geometria nell'orientamento corrente (`fitView`: pan sul centro del box in coordinate vista, margine 10%; prima del primo disegno si ripete in `paintGL`). Entrando in uno schizzo la vista inquadra lo schizzo (tutta la scena se e' vuoto); aprendo un file, tutta la scena. I piani di taglio di `glOrtho` e l'origine di `viewRay` seguono la dimensione della scena (`sceneDepth`).
- **Pan**: tenendo premuto il tasto del pan (di default `Spazio`, *Opzioni → Tasto per il pan...*, in `QSettings` `view/panKey`) il trascinamento col sinistro sposta la vista; anche il tasto centrale del mouse. `panX_`/`panY_` sono una traslazione in coordinate vista applicata prima delle rotazioni (`paintGL`, `projectWorldPoint`, `viewRay`, `screenToSketchPoint` la tengono in conto); funziona anche in modalita' schizzo. Il tasto vince sulle scorciatoie dei menu (`ShortcutOverride`). *Zoom tutto* ricalcola anche il pan.
- **Anteprima delle funzioni**: `requestPreview(definizione, indice)` calcola in background qualsiasi corpo (estrusione, rivoluzione, primitiva, booleana, raccordo) come `rebuildBody` (`computePreview`, dati copiati in `PreviewInputs`: schizzo, forme OCCT copiate, body del kernel proprio), con la chiave `previewKey` per non rifare richieste uguali; al suo posto spariscono il corpo in modifica (`index`) e gli operandi/base (`replaced`). Le finestre usano `PreviewScope` (riga "Anteprima" con lo stato via `setPreviewCallback`, `clearPreview` alla chiusura) e `PreviewSpec` (definizione dai valori della finestra): estrusione, rivoluzione, primitive, booleane e tutti i rami di *Modifica parametri*.
- **Anteprima dei raccordi**: durante la scelta degli spigoli (a ogni clic, e cambiando la *Misura* nella barra di stato, `PdfWindow::pickSizeBox_` → `setEdgePickSize`) e nella finestra della misura (`blendDialog`, a ogni cambio di valore o di tipo) il viewport calcola il raccordo con il kernel attivo in un thread di `QThreadPool` (`requestBlendPreview` → `startPreviewJob`, attesa di 120 ms; la forma OCCT si copia con `BRepBuilderAPI_Copy`, il body del kernel proprio e' immutabile). Il risultato torna nel thread dell'interfaccia (`invokeMethod` su `previewReceiver_`, figlio del viewport) e vale solo se nel frattempo non ci sono richieste nuove (`preview_.generation`); una richiesta uguale a quella in corso o pronta non si rifa'. `drawBlendPreview` lo disegna in ambra al posto della base (e del corpo `hidden`); gli errori vanno nel messaggio della scelta e nella finestra. `clearBlendPreview` alla chiusura della finestra, con Esc e in `restoreDocument`.
- **Modifica degli spigoli di un raccordo**: nella finestra di *Modifica parametri* di un raccordo o smusso, *Spigoli...* (`beginBlendEdit`) torna alla scelta sulla base (disegnata anche se nascosta; `edgePickEdit_` = il raccordo, che al suo posto mostra l'anteprima) con gli spigoli attuali scelti; Invio chiama `setEdgeEditCallback` e la finestra si riapre con gli spigoli nuovi (`updateBody`, un passo di Undo), Esc lascia tutto com'era.
- **Finestre di dialogo spostabili**: `DialogMover` (filtro di eventi su `qApp`, installato nel costruttore di `PdfWindow`) avvia `QWindow::startSystemMove()` quando si preme sullo sfondo o su un'etichetta di una `QDialog`: su Wayland con niri (sessione dell'utente) le finestre non hanno barra del titolo e altrimenti non si potrebbero trascinare.
- **Finestre delle funzioni** (estrusione, rivoluzione, primitive, booleane, misura di raccordo/smusso, *Modifica parametri...*): la conferma esegue il comando (`runUntilApplied`, `askValueUntilApplied`); se fallisce l'errore compare in rosso nella finestra, che resta aperta con i valori inseriti: si esce solo con Annulla o Esc. Nella misura del raccordo *Spigoli...* torna alla scelta degli spigoli con quelli gia' scelti (`resumeEdgePick`); l'ultima misura si ripropone (`PdfWindow::blendSize_`, `edgePickSize`).
- **Modifica dei parametri** (doppio clic sul corpo nell'albero o *Modifica parametri...* nei menu contestuali dell'albero e della vista): la finestra della funzione con i valori attuali (distanza dell'estrusione; asse e angolo della rivoluzione, lo schizzo resta quello; piano, origine e dimensioni della primitiva; raggio/distanza e tipo del raccordo/smusso, gli spigoli restano quelli; operazione della booleana e scambio degli operandi). `CadViewport::updateBody` ricostruisce il corpo e, se riesce, lo sostituisce (un passo di Undo) e rigenera a cascata i corpi che ne dipendono (`regenerateAfter`); altrimenti il corpo resta com'era. Le finestre di rivoluzione e primitive sono funzioni statiche (`revolutionDialog`, `primitiveDialog`) usate anche alla creazione.
- **Eliminazione** (*Modifica → Elimina*, `Canc`; *Elimina* nei menu contestuali): `deleteObject` elimina lo schizzo o il corpo; se altri corpi ne dipendono (a cascata: estrusioni/rivoluzioni dello schizzo, booleane e raccordi che usano il corpo, `withDependentBodies`) li elenca e chiede conferma per eliminarli tutti. `removeBodies` rinumera i riferimenti e rende di nuovo visibili gli operandi rimasti. Un passo di Undo.
- **Corpi e rigenerazione parametrica**: `ExtrusionObject` è estrusione (`sketchIndex`, `distance`) o booleana (`operation`, `firstBody`, `secondBody`, sempre indici minori del proprio). Le modifiche allo schizzo attivo chiamano `sketchEdited()` → `regenerateDependents(sketch)`: ricalcola le estrusioni di quello schizzo e a cascata le booleane che le usano; se fallisce, il corpo ha `error` (voce arancione con ⚠ nell'albero). Durante il trascinamento la rigenerazione avviene al rilascio.
- **Booleane** (`createBoolean`, restituisce l'errore): B-rep esatte tra due solidi qualsiasi (anche piani diversi, anche risultati di booleane). Una superficie (estrusione di un profilo aperto) si puo' intersecare con un solido (in qualsiasi ordine) o tagliare con un solido (differenza superficie − solido): il risultato e' la parte della superficie dentro o fuori; unione, solido − superficie e superficie con superficie danno un messaggio. Gli operandi vengono **nascosti** nello stesso passo di Undo; nell'albero il risultato ha figli informativi "A: …" / "B: …" (`kTreeInfo`).
- **Profondita' di linee e facce**: schizzi, marcatori e spigoli si disegnano nella loro posizione esatta, senza spostamenti in z (uno spostamento lungo Z del mondo spostava nel piano gli schizzi su XZ e YZ e li staccava dai bordi dei solidi e dall'evidenziazione QPainter). Sono le facce dei corpi ad andare indietro con `glPolygonOffset(1, 2)` (`GL_POLYGON_OFFSET_FILL`), cosi' le linee che stanno sulle facce restano visibili. Le croci del cursore sono nel piano dello schizzo, con misura in pixel (`pickTolerance`).
- **Stile di visualizzazione**: `displayMode_` 0 = solo spigoli, 1 = solo facce, 2 = facce + spigoli (`drawExtrusionFaces` / `drawExtrusionEdges` su `BodyDisplay`). Cambiando la qualità si ritassellano i corpi dalla forma esatta.
- **Barre a icone** (`PdfWindow`, costruite dopo tutte le azioni): le icone vanno sulle `QAction` (valgono anche nei menu), il suggerimento aggiunge la scorciatoia (`decorate`). *Modellazione* (`modelingIconBar`): nuovo/apri/salva, annulla/ripeti/elimina, nuovo schizzo (anche sulla faccia), estrusione, rivoluzione, primitive, raccordo, smusso, booleane, zoom tutto, viste standard (le azioni `1`–`6`), stile di visualizzazione. *Strumenti schizzo* (`sketchIconBar`): in modalita' schizzo prende il posto della barra di modellazione (`setSketchModeCallback`), cosi' ci sta anche in una finestra stretta (niri): chiudi lo schizzo, annulla/ripeti, selezione, strumenti di disegno, modifica, costruzione, vincoli (finestra, quota, modalita' della prossima linea A/O/H/V ora esclusive e spuntate, quote L/G), snap, estrusione e rivoluzione. Gli strumenti simili stanno in un pulsante con menu (`flyout`: `QToolButton` `MenuButtonPopup`) che mostra l'ultimo scelto o quello attivo. Stile dei pulsanti (hover, premuto, spuntato, freccia del menu) nel foglio di stile di `setTheme`.
- **Comunicazione con la finestra**: callback `std::function` (`setSelectionCallback`, `setDocumentChangedCallback`, `setPlaneContextCallback`, `setSketchModeCallback`, `setRendererCallback`), non signal/slot. `PdfWindow` non usa `Q_OBJECT`.

## Kernel proprio (`kernel/`, namespace `ForgeCad::Kernel`)

C++17 puro: **né Qt né OCCT** nella libreria `forgekernel`. OCCT si usa solo in `kernel/tests/` come riferimento. L'app lo usa per la modellazione quando è scelto in *Opzioni → Kernel geometrico* (`cad_forge`) e per il banco di prova del menu Debug (`cad_kernel_lab`). File con prefisso `fk_`, test in `kernel/tests/test_*.cpp` (micro framework `fk_test.h`: `FK_TEST`, `FK_CHECK`, `FK_CHECK_NEAR`, `FK_CHECK_THROWS`; generatori casuali con seme fisso e ponte verso OCCT in `fk_test_util.h`).

- `fk_precision.h`: size box alla Parasolid (cubo di 1 km, unità mm), `kLinearResolution` 1e-7, `kAngularResolution` 1e-11.
- `fk_math.h/.cpp`: `Vec<N>` (N = 2, 3; punti e vettori sono lo stesso tipo), `Interval`, `Frame3` (≈ `gp_Ax3`), `Transform3` (con `isSimilarity`, `projectionAlong`).
- `fk_predicates`: `orient2d`/`orient3d` esatti (filtro + espansioni di Shewchuk).
- `fk_bernstein`: polinomi di Bernstein, isolamento di **tutte** le radici in [0,1] (regola dei segni + bisezione). È la base per proiezioni e intersezioni.
- `fk_curve`: `Curve<N>` astratta (`evaluate(t, order, out)`, `evaluateLeft` per i nodi C0, `breakpoints`), `Line`, `Circle`, `Ellipse` (stessa parametrizzazione di `Geom_*`), `TrimmedCurve`, `TransformedCurve` (solo 3D).
- `fk_bspline_basis` (interno, `detail`): funzioni di base (A2.1, A2.3), validazione dei nodi, `ScratchBuffer` (buffer sullo stack: niente allocazioni nel percorso caldo).
- `fk_bspline`: `BSplineCurve<N>` non periodica, razionale o no, con nodi **espansi** (`expandKnots` converte dal formato OCCT). Supporta derivate di ordine qualsiasi (A2.3/A4.2 del NURBS Book), `insertKnot` (A5.1), `clamped`, `bezierSegments` (costo lineare: ogni tratto dalla sua curva locale) con la cache `cachedBezierSegments` (atomica, per curva) e gli archi di conica esatti come NURBS quadratiche (`toBSpline`).
- `fk_curve_algo`: `projectPoint`, `projectPointCandidates` (tutti i minimi locali) e `arcLength`. La proiezione considera anche gli estremi, a differenza di `GeomAPI_ProjectPointOnCurve`. Su B-spline trova tutte le radici di C'·(C−P) con Bernstein; `projectPoint` esamina i tratti in ordine di distanza dal box dei poli e si ferma quando il limite inferiore supera il minimo trovato (indispensabile sulle curve tracciate, con centinaia di tratti). Le curve trasformate da una similitudine si proiettano sulla base. `arcLength` usa Gauss-Kronrod 7-15 adattivo.
- `fk_surface`: `Surface` astratta (`evaluate(u, v, order, out)` con `out[derivativeIndex(k, l, order)]`, `normal`, `uIso`/`vIso` esatte o `nullptr`, breakpoint), `Plane`, `CylindricalSurface`, `ConicalSurface`, `SphericalSurface`, `ToroidalSurface`, `ExtrusionSurface`, `RevolutionSurface`. Parametrizzazioni identiche a quelle di OCCT.
- `fk_bspline_surface`: `BSplineSurface` a prodotto tensoriale (A3.6/A4.4). Poli `[i * vPoleCount + j]` come `Poles(i+1, j+1)` di OCCT. Isoparametriche esatte e `bezierPatches`.
- `fk_surface_algo`: poli (`surfacePoles`: poli della sfera, vertice del cono, estremi del meridiano sull'asse; `poleIndex`), `poleWalk` (dove un loop arriva in un polo e ne riparte, nello spazio (u, v) cammina lungo la linea del polo: verso u minori se il polo sta sopra e la faccia ha il verso della superficie, maggiori altrimenti; la variante con i valori di u vicino al polo decide il giro per i tratti tangenti nel polo) e `normalAt` (nel polo la media delle normali di un anello vicino, dalla parte indicata: nel vertice del cono l'asse). `projectPoint` su superficie, bordi compresi (isoparametriche dei bordi e degli spigoli interni proiettate in modo esatto). Il metodo per l'interno dipende dal tipo:
  - piano e cilindro: forma chiusa;
  - cono, sfera, toro: punti stazionari in forma chiusa;
  - B-spline: branch-and-bound sulle pezze di Bézier (limite inferiore dal box dei poli e dal limite direzionale min (p−qᵢ)·n);
  - estrusione: riduzione esatta alla curva base proiettata, anche con v infinito;
  - rivoluzione con meridiano piano: riduzione esatta al meridiano;
  - resto (meridiano sghembo): griglia + Newton.

- `fk_topology`: `Body` B-rep alla Parasolid (region → shell → face → loop → fin → edge → vertex), entità in vettori con id tipizzati (`VertexId`, `FinId`, …; il body si copia per valore), tolleranze locali su vertici ed edge, facce periodiche senza cucitura. Operatori di Eulero con le loro inverse: `mvfs/kvfs`, `mev/kev`, `mef/kef`, `kemr/mekr`, `kfmrh/mfkrh`, `semv/jekv`. Convenzione: la faccia sta a sinistra della fin (loop esterno antiorario attorno alla normale, fori orari).
- `fk_body_check`: `checkBody` (topologia, formula di Eulero-Poincaré, vertici sulle curve, edge sulle superfici, orientamento dei loop delle facce piane) e `shellGenus`.
- `fk_curve_ops`: `reversedCurve` (R(s) = C(−s), stesso tipo) ed `embedCurve` (curva 2D del piano di un `Frame3` portata in 3D, stesso tipo e parametro). `Body::reverseEdge` le usa per girare un edge.
- `fk_quadrature.h`: Gauss-Kronrod 7-15 adattivo con soglia di arrotondamento alla QUADPACK (senza, gli integrandi grandi facevano scendere la bisezione fino in fondo).
- `fk_profile`: `ProfileSegment`/`ProfileLoop`/`ProfileRegion`. `buildProfile` concatena i tratti per estremi entro tolleranza (in qualsiasi ordine e verso) e li annida come lo schizzo dell'app (profondità pari = materiale). Poi `signedArea`, `area` e `windingNumber`, calcolati come integrali esatti sulle curve.
- `fk_extrude`: `makeExtrusion(frame, region, height)`: base e coperchio piani, lati piani (segmenti), cilindrici (archi) o `ExtrusionSurface` (ellissi, spline, NURBS). Un loop di una sola curva chiusa dà un fianco con due loop e senza cucitura. Gli estremi che si toccano solo entro la tolleranza diventano vertici (ed edge verticali) tolleranti.
- `fk_primitives`: `makePrism`, `makeBox` e `makeCylinder` sono casi particolari di `makeExtrusion`. C'è anche `assignPolyhedralGeometry`.
- `fk_pcurve`: SP-curve delle fin (`Fin::pcurve`, stesso parametro dell'edge: S(p(t)) = C(t); `pcurveTolerance` 0 = esatta). `exactPCurve`: sul piano la curva nel sistema del piano (stesso tipo); altrove si ipotizza (u, v) affine in t dai parametri di alcuni punti e lo si verifica in 3D (isoparametriche, generatrici, curve base delle estrusioni, eliche) → `Line<2>` se la pendenza è unitaria, altrimenti B-spline di grado 1. `fitPCurve`: B-spline cubica C1 (Hermite a tratti, nodi interni tripli) raffinata finché lo scarto 3D è sotto la tolleranza (`kPCurveTolerance` 1e-7). Negli estremi che cadono in un polo u vale il limite lungo la curva (campioni ed Hermite). `computePCurves(body)` le assegna alle fin che non le hanno; `loopPoleWalks` da' i cammini di un loop lungo i poli; `makeExtrusion` la chiama (tutte esatte). `reverseEdge` e `semv` le aggiornano; `checkBody` le verifica (`PCurveOffEdge`). Chi cambia la geometria di un body deve azzerarle e ricalcolarle. `invertPoint` (in `fk_surface_algo`) è l'inversione condivisa: Newton dalla stima, poi proiezione, parametri periodici srotolati vicino alla stima.
- `fk_intersect`: `planeRoots` (radici di n·C(t) = c per tutte le curve, Bernstein sulle B-spline, tratti coincidenti a parte; anche i punti in cui la curva tocca il piano senza attraversarlo, dai minimi di |p| entro la tolleranza, e lo stesso per l'equazione implicita delle coniche: le radici doppie si perdono con i soli cambi di segno), `intersectCurves` (curva-curva nel piano: retta o conica in forma chiusa/implicita, B-spline-B-spline per suddivisione di Bezier + Newton; sovrapposizioni segnalate: i tratti comuni finiscono negli estremi o nei nodi delle due curve (tratti polinomiali che coincidono su un intervallo coincidono su tutto il tratto), che si provano se la suddivisione non si esaurisce; punti vicini uniti da un arco entro la tolleranza sono un solo contatto tangente; con `distinctParameters` tiene i punti uguali con parametri diversi), `intersectPlaneSurface` (piano con piano, cilindro, sfera, estrusione: rette, cerchi, ellissi; cono solo con un piano per il vertice (generatrici) o perpendicolare all'asse (cerchio); sezione obliqua di un'estrusione = proiezione obliqua della curva base, NURBS esatta; rette di tangenza separate), `intersectLineSurface` (per il raggio del punto-in-solido), `intersectCurveSurface` (curva con piano, cilindro o estrusione: riduzione esatta a curva-curva nella sezione normale alla direzione, con `planarImage` che proietta una curva sul piano conservandone il tipo o passando per la NURBS esatta). `GeneralizedCylinder` vede cilindri ed estrusioni come C(u) + v D.
- `fk_marching`: `intersectSurfaces(a, b, bounds, seeds)` tra due superfici non piane. Tracciamento generale: predittore lungo n_A × n_B, correttore di Newton su (u, v, s, t) con il piano normale all'avanzamento; il passo si adatta finché la cubica di Hermite (curva 3D e SP-curve sulle due superfici) resta entro `fitTolerance` (1e-9) dall'intersezione vera in tre punti interni. I nodi delle superfici e i bordi dei domini non periodici diventano punti della curva (sugli spigoli vivi, come la chiusura ad angolo di una spline chiusa, la curva fa un angolo: tangenti in arrivo e in partenza separate). Risultato: B-spline cubiche C1 con le due SP-curve, chiuse o aperte. Semi completi per i cilindri generalizzati: n·C_A(u) = n·C_B(s) con n = D_A × D_B è separabile, ogni componente chiusa passa per una generatrice critica (n·C' = 0, o nodo della curva) di A o di B; le componenti che attraversano il bordo delle facce partono dai punti edge-superficie (passati dalle booleane). Direzioni parallele: rette esatte dalle sezioni (una tangenza che attraversa, a un flesso, è una retta di taglio); rette di tangenza segnalate; sezioni sovrapposte in un tratto: `coincident` e le rette agli estremi della striscia comune (dove le superfici si separano, anche tangenti) sono tagli. Punti di tangenza (normali parallele): stanno sulle generatrici critiche di entrambe, quindi sono semi; la differenza delle seconde forme fondamentali decide tra contatto isolato (`isolatedPoints`, definita), rami che si incrociano (`singularPoints`, indefinita: le due direzioni nulle; i punti si registrano tutti prima di tracciare, ogni semiramo parte dal punto e ogni tracciamento si ferma quando ne raggiunge uno, `splitParameters` dà i parametri dei vertici) e contatto di ordine superiore (`tangentPoints`, non gestito). Se la forma è semidefinita (una sola direzione in cui le curvature coincidono) si prova a seguire una curva di tangenza (sfera in un cilindro dello stesso raggio, toro attorno a un cilindro che ne tocca l'equatore): predittore lungo la direzione nulla, correttore di Gauss-Newton sui 5 residui più un piano (smorzamento minimo: senza piano il sistema ha una direzione libera), cubiche di Hermite verificate come le curve; va in `tangentCurves` e i semi che vi cadono si saltano. Un seme con seno sotto 1e-3 cerca prima un punto di tangenza vicino (se Newton finisce lontano dal seme, oltre 1e-3 della scala, il seme regolare si traccia comunque). Anse a otto (un cilindro che tocca l'interno dell'altro) comprese. Superfici di rivoluzione coassiali (cilindri, coni, sfere, tori, rivoluzioni con il meridiano in un piano per l'asse; due sfere: la retta dei centri): niente tracciamento, i cerchi dei punti comuni dei profili nel semipiano (rho, z), esatti; profili tangenti senza attraversarsi = cerchi di tangenza; tratti di profilo comuni = `coincident` con i cerchi agli estremi come tagli (`coaxialRotational`). Contatti di ordine superiore (le superfici coincidono al secondo ordine): un tracciamento fermato vicino al contatto (`Stop::Tangent`) si completa fino al punto (`completeToContact`): il punto con `refineSingular` (normali nei poli dall'anello attorno), agganciato a un polo vicino o a un contatto gia' raggiunto da un altro ramo (lungo le direzioni piatte e' mal determinato); poi la corda dall'ultimo nodo al punto se sta su entrambe entro 1e-8 (vicino al contatto le superfici si scostano dal piano tangente solo al secondo ordine; nel polo la corda segue un meridiano, u costante), altrimenti punti su piani normali a distanze decrescenti (previsione in sqrt(s), Levenberg-Marquardt dove Newton e' mal condizionato); il punto e' un vertice (`singularPoints`) e i rami tangenti tra loro si distinguono dal lato della corda. Rami che attraversano il contatto (sella di scimmia) si tracciano come curve lisce. B-spline coincidenti in parte con l'altra superficie (`splineCoincidence`): pezze di Bezier classificate con 5 x 5 campioni (sopra l'altra con la stessa normale, un blocco 2 x 2 esclude le curve di tangenza); `coincident` e tagli sulle linee di nodo dove da una parte le superfici coincidono e dall'altra no (tratti esatti con `curveOnSurface`); niente suddivisione, solo i semi degli edge fuori dalla zona comune. Vertice di un cono che sta sull'altra superficie: punto singolare registrato prima del tracciamento, con i rami lungo le due generatrici nel piano tangente dell'altra (sul cono u fisso, v lungo la generatrice); le curve vi si fermano. Per le altre coppie (sfera, cono, toro, rivoluzione, B-spline, anche con un piano) i semi vengono dalla suddivisione: le pezze di Bézier delle forme NURBS (finestra dei parametri dal box: periodo intero, dominio, o angoli del box per piano, cono e cilindri generalizzati) si dividono finché i box dei poli si toccano e sono sotto 1/50 del box, poi Newton a norma minima sulle superfici vere; il tracciamento è lo stesso. I quadranti delle superfici analitiche (e la rotazione delle rivoluzioni) non sono nodi per il tracciamento: la superficie lì è liscia (una curva che giace su una di quelle linee, come la sezione di un toro con un piano per l'asse, non si poteva tracciare).
- `fk_nurbs`: forme NURBS esatte per gli algoritmi sui poli: `rationalBezierPieces(curva, range)` (rette, coniche, B-spline, curve limitate e trasformate) e `toBSplineSurface(superficie, u, v)` (piano bilineare; cilindri, coni, sfere, tori e rivoluzioni, anche con meridiano sghembo, come prodotto tensoriale del cerchio unitario NURBS e del meridiano; estrusioni; B-spline). Il parametro NURBS delle coniche non è quello originale: i risultati si riportano per proiezione.
- `fk_curve_surface`: curva-superficie per le superfici che `intersectCurveSurface` non riduce al piano. Sfera, cono (due fogli) e toro: equazione implicita omogenea (grado 2 o 4) composta con i tratti di Bézier razionali → radici di Bernstein (anche tangenze entro la tolleranza), poi Newton su F(C(t)) nel parametro della curva (le radici su un tratto lungo hanno solo la sua precisione relativa). B-spline e rivoluzione: suddivisione di tratti e pezze di Bézier (`patchBox`, `splitPatch`) + Newton su (s, u, v). `intersectCurveSurface` e `intersectLineSurface` ora gestiscono tutti i tipi. `curveOnSurface`: tratti di una curva che stanno su una superficie (campioni, bisezione, estremi agganciati ai bordi del dominio, alle linee di nodo delle B-spline, ai nodi della curva o a linee date); la suddivisione lo usa quando un tratto giace sulla superficie.
- `fk_revolve`: `makeRevolution(frame, regione, angolo)` parziale: il giro completo intersecato con il cuneo dei due semipiani per l'asse (prisma poligonale, booleana esatta); `makeSphere`, `makeCone` (anche tronco o rovescio), `makeTorus`. `makeRevolution(frame, regione)`: giro completo di una regione del semipiano x ≥ 0 del piano XZ di `frame` attorno a Z. Un tratto → una faccia (piano, cilindro, cono, sfera con il centro sull'asse, toro, `RevolutionSurface`); i tratti sull'asse non danno facce; ogni vertice fuori dall'asse → edge circolare chiuso (un loop di una sola curva chiusa: nessun vertice, toro intero). Facce senza cucitura, con un polo se un estremo sta sull'asse, o senza loop (sfera intera). `Body::build` accetta facce senza loop (loop di un vertice isolato).
- `fk_blend`: `blendEdges(body, edge, misura, smusso)` e `nearestEdge`. Ogni spigolo nella sua sezione (piano normale a uno spigolo rettilineo, semipiano per l'asse di uno circolare, chiuso o arco): le facce vi sono rette o cerchi (piani; cilindri paralleli allo spigolo; per gli spigoli circolari piani perpendicolari, cilindri, coni, sfere, tori coassiali), il raccordo e' il cerchio tangente alle due (curve spostate del raggio dalla parte dell'angolo minore), lo smusso la corda tra i punti a distanza data. La zona tra spigolo e raccordo, estrusa lungo lo spigolo o ruotata attorno all'asse (giro completo, o l'angolo dell'arco con `makeRevolution` parziale; scostata di 5% dalle facce sulle rivoluzioni e nelle catene: niente facce coaxiali coincidenti con il toro tangente), si toglie (spigolo convesso) o si aggiunge (concavo) con le booleane. Estremi: la zona finisce nel piano normale allo spigolo se le altre facce del vertice vi sono normali o continuano tangenti una delle due facce; catene di spigoli tangenti (segmenti e archi di un contorno raccordato, anche chiuse) hanno gli utensili cuciti in uno (`sewBodies`: le sezioni comuni, facce piane uguali e opposte, spariscono; poi `unifySameDomain`), perche' la booleana tra cilindro e toro tangenti lungo il cerchio comune non riesce; contro una faccia piana obliqua (vertice con tre facce) la zona si allunga oltre il vertice e si taglia con il semispazio della faccia. Due spigoli scelti in un vertice: angolo a mitra come OCCT (verificato, anche con facce oblique e arco + segmento). Tre spigoli rettilinei convessi tra tre piani: pezza d'angolo costruita direttamente (`buildPiece` con `Body::build`): raccordo = cella tra vertice e centro della sfera tangente ai tre piani meno la palla (i raccordi degli spigoli finiscono nei piani per il centro normali agli spigoli), smusso = tetraedro tra il triangolo per i punti d'incontro dei bordi degli smussi e il punto comune dei tre piani di smusso (come OCCT negli angoli retti; negli angoli non retti OCCT mette una pezza B-spline). Verificato contro OCCT e contro i volumi esatti: box con 3 e 12 spigoli, prismi trapezoidali, rettangolo arrotondato (bordo superiore), tasca arrotondata (bordo e fondo), profilo a D. I bordi di forma libera vanno a `fk_blend_loop` (sotto) con i segmenti e gli archi che li continuano nello stesso loop (`planarChainRuns`); gli altri spigoli poi, sul risultato. Non gestiti: facce di altri tipi (raccordi tra superfici curve qualsiasi), estremi contro facce curve non normali e non tangenti, piu' di tre spigoli in un vertice, pezze d'angolo con facce o spigoli curvi.
- `fk_blend_loop`: raccordi e smussi a palla rotolante dei bordi di forma libera (spline, NURBS, ellissi; anche segmenti e archi nella stessa catena) tra una faccia piana T e i fianchi paralleli alla sua normale (piani, cilindri, estrusioni lungo la normale). Sezione esatta: quarto di cerchio NURBS quadratico (peso √2/2 nel punto di mezzo, che e' il bordo stesso) o segmento per lo smusso; lungo il bordo tre curve di controllo (traslata sul fianco, bordo, parallela in T) come cubiche di Hermite a tratti con gli stessi nodi (tripli), raffinate finche' si scostano meno di 1e-9 dalle vere (la parallela di una spline non e' una B-spline: come OCCT, e come le curve del marching). Niente booleane: chirurgia topologica su `Model` (le liste di `Body::build`): T si accorcia sulla parallela, il fianco sulla traslata (isoparametriche v = 1 e v = 0 della superficie nuova), gli spigoli verticali tra fianchi e quelli di estremita' si accorciano (`moveEnd`), si aggiungono le facce dei raccordi. Catene = tratti consecutivi di edge scelti in un loop di T (un loop di un solo edge chiuso si divide prima con `semv`). Vertici: lisci (tangenti continue) -> i raccordi si incontrano sull'arco della sezione (isoparametrica u esatta); angoli vivi convessi con uno spigolo verticale -> i due raccordi si tagliano lungo la loro intersezione (`intersectSurfaces` su due superfici rifatte solo vicino all'angolo, con un box stretto e un seme interno da Newton; il punto in T e' l'intersezione esatta delle due parallele, dove i raccordi sono tangenti tra loro: li' la curva ha solo la precisione del secondo ordine e vertici/edge diventano tolleranti per lo scarto); estremi di catene aperte contro un piano normale al bordo -> vi resta l'arco della sezione. Controlli: parallela senza cuspidi (raggio minore del raggio di curvatura), punti delle curve nuove dentro T e dentro i fianchi, `checkBody` sul risultato. Verificato con i volumi esatti (sezione A che scorre lungo una curva piana: V = A L - M ∫κ ds, con M momento della sezione rispetto al bordo; ellisse, spline chiusa, tasca ellittica bordo e fondo, spline aperta tra fianchi normali, a 1e-9) e, forzato su segmenti e archi, contro il percorso delle booleane (parallelepipedo con 4 angoli vivi, trapezio con angoli acuti e ottusi, rettangolo arrotondato: 1e-8). OCCT sui prismi da spline non fa da riferimento: `BRepGProp` sbaglia il volume del solido di partenza (anche del 2%) e sul contorno spline + segmenti il suo raccordo esce sbagliato. Non gestiti: angoli vivi concavi, estremi contro facce non normali, fianchi non paralleli alla normale di T.
- `fk_classify`: `classifyPointOnFace` (semiretta verticale nello spazio (u, v) contro le SP-curve e i cammini dei loop lungo i poli; sul cono un punto dell'altra falda e' fuori; la prima incontrata dice da che parte sta il dominio, funziona sulle superfici periodiche senza cucitura; sul toro fa il giro in v e, se la verticale non incontra loop, prova l'orizzontale; nessun loop incontrato su una superficie chiusa: dentro se i loop sono fori), `faceBox` (per coni, tori, rivoluzioni e B-spline dal box dei poli NURBS sulla finestra della faccia, poli e vertice compresi: le facce escono dal box dei loro edge), `SolidClassifier` (parità delle intersezioni di un raggio, cambia direzione se sfiora facce o edge) e `firstRayHit` (primo punto colpito da una semiretta, e la sua faccia: la selezione a video dell'app).
- `fk_tessellate`: `tessellate(body, {deflection, angle})` → triangoli per faccia (punti, normali della faccia, parametri (u, v)) e polilinee degli edge, SOLO per disegnare. Edge campionati sulle curve esatte (scarto dalla corda e angolo tra le tangenti; si parte dai soli nodi in cui la tangente gira, un intervallo con più di 10 nodi si divide comunque). Faccia: campioni degli edge portati sulle SP-curve; la finestra (u, v) di un periodo con il taglio nel più grande intervallo libero, divisa in celle anche dagli spigoli vivi della superficie (nodi dove la normale salta); i loop si spezzano sui bordi delle celle (semiaperti: un campione sul bordo sta nella cella sopra) e le catene si ricompongono lungo il bordo della cella in senso antiorario (comprende poli e vertice del cono, i cui lati si dividono finché la normale cambia poco). I loop che passano per un polo camminano lungo la sua linea (tratti rettilinei in (u, v), infittiti con l'angolo). Poligoni che ripassano per lo stesso punto (loop pizzicati) divisi in poligoni semplici, poi con i fori triangolati con earcut (senza togliere i punti allineati: sui bordi isoparametrici lo sono solo in (u, v)), poi raffinati dividendo i lati interni troppo lontani dalla superficie (distanza dal segmento, non dal punto medio: la velocità del parametro non è uniforme) o con normali troppo diverse, con scambi di Delaunay nel piano (u, v) scalato (mappa di adiacenza aggiornata a ogni scambio). `triangulatePolygon` è earcut da solo.
- `fk_boolean`: `booleanOperation(a, b, op)` con `op` 0 unione, 1 intersezione, 2 differenza (come l'app). Archi d'intersezione faccia-faccia (con un piano: `fk_intersect`; tra cilindri ed estrusioni: `fk_marching`, con le SP-curve del tracciamento assegnate alle fin del risultato) limitati alle due facce → divisione di edge e archi nei loro estremi → ogni faccia divisa dal grafo dei tagli (cicli con la faccia a sinistra, ordine angolare attorno alla normale; raggruppamento in esterni/fori/fasce sui poligoni (u, v)) → classificazione dei pezzi (prima le facce complanari: sopra con verso uguale/opposto; poi il verso della faccia che taglia; poi un raggio) → selezione e cucitura con `Body::build` → `computePCurves` e `checkBody` (se fallisce: `std::domain_error`, mai un body sbagliato). Tolleranza di default 1e-6. Gestisce facce complanari, fori ciechi e passanti, corpi annidati o disgiunti, booleane in cascata, cilindri e fianchi estrusi tra loro in qualsiasi direzione (fori incrociati, innesti a T) e i contatti tangenti: rette di tangenza e contatti isolati non dividono le facce; i tagli che stanno sul bordo della faccia che taglia non guidano la classificazione locale (si usa il raggio); gli speroni (tagli percorsi avanti e indietro) si tolgono; un ciclo che passa due volte per un nodo si divide; gli edge usati da più di due fin (solidi che si toccano lungo uno spigolo) si dividono accoppiando le facce attorno all'edge per settori di materiale e i vertici con più "ombrelli" di facce si sdoppiano, così il risultato resta una varietà (due shell che si toccano). Nel grafo delle facce le direzioni quasi coincidenti (entro 1e-6, anche con il bordo entrante) si ordinano con corde alla stessa distanza dal nodo per tutti i tratti. Piano con cono, toro, rivoluzione o B-spline passa dal marching. Facce periodiche: i cicli avvolti in u si raggruppano in fasce tra due cicli, tra un ciclo e un polo (sfera, vertice del cono, meridiano sull'asse) o attraverso il taglio della direzione v periodica (toro); i cicli avvolti in v (toro) allo stesso modo lungo u; sulle superfici chiuse (sfera, toro) una regione senza contorno esterno è "tutta la superficie meno i fori" (`SubFace::whole`), anche senza cicli (sfera intera: si classifica con un punto qualsiasi della superficie). Le semirette nello spazio (u, v) fanno il giro nelle direzioni periodiche e diventano orizzontali per i cicli avvolti in v. Facce su superfici coincidenti (`sameSurface` di `fk_surface_algo`: piani, cilindri coassiali dello stesso raggio, sfere, coni e tori uguali, fianchi estrusi in direzioni parallele dalla stessa curva) si trattano come le complanari: il bordo di ciascuna la taglia dove passa dentro l'altra (attraversamenti nello spazio (u, v) della faccia tagliata, SP-curve anche spostate di un periodo con `translatedCurve`), classificazione con le normali delle due facce. Un edge che giace sulla superficie curva dell'altro solido (generatrice comune a due fianchi paralleli) dà i suoi estremi come punti di divisione; un taglio isolato percorso avanti e indietro (contatto lungo una retta) non forma un ciclo. Poli: le curve d'intersezione si dividono nei poli delle due superfici (sfera, vertice del cono, meridiano sull'asse), che restano vertici anche dopo la fusione degli edge; nello spazio (u, v) i cicli camminano lungo la linea del polo (`poleWalk`) e la normale nel nodo del polo e' quella dell'anello attorno, dalla parte della faccia (le due falde del cono). Piano per il vertice del cono: generatrici esatte. Fianchi estrusi paralleli o superfici di rivoluzione coassiali che coincidono solo in parte (sezioni con un tratto comune, anche con una separazione tangente in un nodo): le rette agli estremi della striscia comune tagliano entrambe le facce, gli edge dell'altra faccia tagliano solo nei tratti che stanno sulla superficie (i cerchi coassiali tutti o niente, altrimenti `intersectCurveSurface(...).coincident`) e i pezzi sulla striscia si classificano con le normali come i complanari. Curve tracciate che si incrociano tra loro senza che il tracciamento lo segnali (i cerchi di Villarceau di un piano bitangente al toro, tracciati come curve chiuse) si dividono nei punti comuni (dalle SP-curve); sul toro un contorno che si tocca in un nodo e si scomporrebbe in due cicli avvolti nelle due direzioni resta un ciclo solo (disco pizzicato). Contatti di ordine superiore in un punto: se su un anello attorno la distanza dall'altra superficie non cambia segno e' un contatto isolato (non divide le facce); se ne partono rami va bene solo se una curva tracciata passa per il punto. Lamine: lamina con solido (intersezione, anche con il solido per primo; differenza lamina − solido) da' la parte della lamina dentro o fuori, `Body::buildSheet`; unione, solido − lamina e due lamine: eccezione. Poi `unifySameDomain` (disattivabile con `BooleanOptions::unifySameDomain`, funziona anche sulle lamine). Rami da un contatto di ordine superiore (cilindro tangente nel polo piatto di una cupola: quattro rami a cuspide, volume esatto) completati dal marching; nei poli un ciclo che vi ripassa non si divide. B-spline coincidenti in parte: la zona comune come per le superfici coincidenti, con i tagli sulle linee di nodo; i tratti comuni degli edge da `curveOnSurface` agganciati ai tagli; tagli uguali da curve diverse (isoparametrica esatta e curva tracciata) riconosciuti proiettando il punto medio; estremi uniti solo entro la tolleranza della booleana: edge tolleranti. Non gestisce (eccezione): cicli avvolti nelle due direzioni del toro che non delimitano un disco pizzicato; curve trasversali chiuse fuori da una zona comune di B-spline che non toccano gli edge. Nelle booleane con contatti tangenti OCCT sbaglia (anche volumi negativi): i test usano volumi esatti (griglie di blocchi e cilindri allineati, Steinmetz).
- `Body::build` costruisce un body da vertici, edge e facce per indici (usato dalle booleane e da `fk_unify`); le `BuildFin` possono portare le SP-curve. `Body::buildSheet` fa lo stesso per le lamine (sheet body): region non solida, edge di bordo con una sola fin (`isLaminar`; `edgeStart`/`edgeEnd` funzionano anche lì), `isSheet()`. `checkBody` le accetta (niente Eulero sulle shell non solide); `massProperties` le rifiuta (usare `faceArea`). `makeSheetExtrusion(frame, catene, altezza)` estrude catene aperte (`Profile::chains`, prolungate nei due versi da `buildProfile`).
- `fk_unify`: `unifySameDomain(body)`, come `ShapeUpgrade_UnifySameDomain`: facce adiacenti sulla stessa superficie (stesso oggetto, o `sameSurface` con le normali concordi) diventano una (gli edge interni spariscono, i loop si ricollegano girando attorno ai vertici con il `next` della fin gemella); poi due edge in un vertice usato solo da loro sulla stessa curva (stesso oggetto con tratti contigui, rette allineate, lo stesso cerchio) diventano uno. Le booleane la applicano sempre; i test confrontano facce ed edge (senza cuciture) con OCCT dopo la sua fusione. Le facce senza edge (sfera, toro interi) restano; i vertici nei poli di una faccia non si tolgono (li' l'SP-curve salta). Funziona anche sulle lamine (gli edge di bordo restano).
- `ExtrusionSurface` di una curva chiusa non periodica (spline chiusa) è periodica in u con periodo = lunghezza del dominio: `evaluate` riporta u nel dominio.
- `fk_mass`: `massProperties(body)` (volume, area, baricentro, tensore d'inerzia come `GProp_GProps::MatrixOfInertia`) e `faceArea`, sulle superfici esatte. Divergenza (integrali di flusso sulle facce), poi Green nello spazio (u, v): \oint −G du con G = \int f dv calcolata con una quadratura interna. Le curve dei loop in (u, v) sono le SP-curve esatte o approssimate entro 1e-8 (quelle del marching; traslate di periodi interi per restare continue lungo il loop); altrimenti si inverte la superficie sui punti degli edge. Su un edge che sta sulla superficie solo entro la sua approssimazione l'inversione avrebbe un rumore che blocca la quadratura; per la stessa ragione la "grandezza" di du tiene conto dell'arrotondamento della derivata delle SP-curve sui tratti corti (il tracciamento evita comunque tratti sotto 1e-5 della scala). Facce periodiche senza cucitura: il termine in du non vede le cuciture. Sul toro, una faccia con loop avvolti solo in v si integra con i parametri scambiati (`SwappedSurface`, `SwappedCurve`: il verso del dominio si inverte, la normale resta quella di S), così i loop diventano avvolti in u (il caso delle fasce). Poli (sfera, vertice del cono) e facce del toro si chiudono aggiungendo le fasce complete di parametro; un loop che passa per un polo aggiunge il termine −∫G du lungo il cammino sulla linea del polo. La grandezza di N nell'integrando comprende l'arrotondamento delle derivate (vicino al vertice del cono Su nasce da una cancellazione: senza, la quadratura interna non si fermava). Non gestiti (lancia `std::domain_error`): loop avvolti nelle due direzioni del toro, facce aperte o illimitate, lamine.
- `fk_quadrature.h` ha anche la versione vettoriale (`integrateVector`, N integrali sugli stessi nodi). Ogni valore porta una "grandezza" che fissa l'errore di arrotondamento: senza, le componenti che si annullano per cancellazione (x·n su un piano che passa per il riferimento, du lungo un edge in v) mandavano la bisezione fino in fondo.

Attenzione a `BRepGProp` di OCCT come riferimento: senza `Eps` usa una regola di Gauss a ordine fisso, imprecisa sulle curve razionali. `VolumeProperties`, anche con `Eps`, sbaglia fino a qualche percento sui prismi con fianchi estrusi da spline (verificato: area vera 6.358333, volume OCCT −0.7% di default e −3% adattivo). Nei test il volume di riferimento di un prisma è l'area adattiva del profilo per l'altezza; le lunghezze si misurano con `GCPnts_AbscissaPoint`. Anche `SurfaceProperties(face, props, 1e-13)` è affidabile solo per l'area: su una faccia con un foro a spline sbaglia I_xx di 3e-6 (relativo) e il baricentro di 1e-8. Per i momenti di un prisma i test usano il teorema di Green lungo le curve del profilo (`greenMoments` in `test_mass.cpp`); i solidi analitici (cono, sfera, toro) hanno riferimenti in forma chiusa. Il nuovo kernel coincide con questi a 1e-11.

Il kernel e i suoi test si compilano sempre con `-O2 -g`, anche in Debug: la suite completa gira in circa 100 s (con la CPU a piena frequenza; a batteria anche il triplo) (quasi tutto nei test delle booleane, che confrontano con BRepAlgoAPI e con una tassellazione fine di OCCT: quest'ultima da sola prende più di 10 s). Anche l'area di `BRepGProp::SurfaceProperties` sbaglia (fino allo 0.06%) sulle facce estruse da spline tagliate da un piano: lì i test usano come terzo parere l'area e il volume della tassellazione fine, e l'identità V(A∪B) = V(A) + V(B) − V(A∩B).

Roadmap: 0–8 fatti (infrastruttura, curve, superfici, topologia B-rep con controllo di validità, estrusione di profili qualsiasi, proprietà di massa, SP-curve, intersezioni con un piano e booleane, marching tra superfici non piane con le booleane tra cilindri e fianchi estrusi), più contatti tangenti, fusione delle facce/edge sulla stessa geometria, integrazione nell'app (menu Opzioni, tassellazione e selezione proprie), superfici coincidenti non piane, lamine, rivoluzione, booleane con sfere, coni, tori, rivoluzioni e B-spline (semi per suddivisione), contatti tangenti lungo curve, curve per i poli e per il vertice del cono (piani), fianchi estrusi coincidenti in parte, cerchi di Villarceau, contatti isolati di ordine superiore, booleane lamina-solido, curve tracciate per il vertice del cono, rivoluzioni coassiali (esatte, anche coincidenti in parte), rivoluzioni parziali e primitive di rivoluzione (anche nell'app, con le linee di costruzione), rami da un contatto di ordine superiore, B-spline coincidenti in parte, raccordi e smussi (anche nell'app), con archi aperti, catene tangenti, estremi obliqui, pezze d'angolo (sfera / triangolo) e bordi di forma libera tra una faccia piana e i suoi fianchi (palla rotolante). Seguono i raccordi tra superfici curve qualsiasi e gli angoli vivi concavi delle catene. Ogni nuovo modulo va con i suoi test contro OCCT.

## Albero modello (`PdfWindow`)

L'albero è ricostruito da `rebuildModelTree()` a partire da `viewport_->sketches()` / `extrusions()`: le prime 4 voci (origine e piani) sono fisse, le altre vengono rigenerate. `Qt::UserRole` = tipo (`TreeItemType`: `kTreeOrigin` 0, `kTreePlane` 1, `kTreeSketch` 3, `kTreeExtrusion` 4) e `Qt::UserRole+1` = indice nel vettore del viewport. La casella di spunta controlla la visibilità (`suppressTreeClick_` evita che il clic sulla casella apra lo schizzo). Nomi: "Schizzo N" / "Estrusione N" / "Unione N" con N = numero di elementi + 1.

## File e impostazioni

- `./forgecad documento.prt` apre il documento all'avvio (`PdfWindow::openDocumentPath`).
- *File → Nuovo / Apri... / Salva / Salva con nome...* (`Ctrl+N`, `Ctrl+O`, `Ctrl+S`, `Ctrl+Shift+S`): documenti `.prt` (`cad_document_io`). `PdfWindow` tiene `documentPath_` e `documentModified_` (impostato dal callback `documentChanged`, tranne durante `loadingDocument_`); titolo con `*` se modificato; alla chiusura, a Nuovo e ad Apri chiede se salvare (`maybeSaveChanges`). `CadViewport::loadDocument` esce dallo schizzo, azzera la cronologia, ricampiona le curve e rigenera tutti i corpi.
- *Opzioni → Salva impostazioni dell'interfaccia*: gruppo `interface` di `QSettings` (geometria e stato della finestra, stile, luci, qualita', sfondo, piani, griglia, zoom con la rotella, assi, antialiasing), ripreso alla fine del costruttore di `PdfWindow` attivando le azioni dei menu. *Ripristina impostazioni predefinite...* cancella il gruppo (e le chiavi `view/*`), valido dal prossimo avvio. Dock e barre hanno `objectName` per `saveState`.

## Scorciatoie

- `Spazio` (modificabile) + trascinamento, o tasto centrale: pan.
- `1`–`6`: viste Front/Rear/Right/Top/Isometric/Trimetric secondo l'orientamento degli assi (disabilitate in modalità schizzo).
- `K`: finestra dei vincoli.
- `+`, `-`, `0`: zoom (anche con la rotella, se attiva).
- `Ctrl+E`: estrusione.
- `Canc` elimina (corpo/schizzo selezionato, o entita' dello schizzo); `D` quota del segmento selezionato.
- `Ctrl+Z` annulla, `Ctrl+Shift+Z` / `Ctrl+Y` ripete, `Ctrl+Shift+H` mostra tutti gli oggetti.
- Scelta degli spigoli (raccordo/smusso): clic, `Invio` conferma, `Esc` annulla.
- Modifica dello schizzo: `T` taglia, `E` estendi, `S` spezza, `R` raccordo, `M` smusso.
- `C` (in modalita' schizzo): entita' selezionate di costruzione / normali.
- Vincoli: `A` automatico, `O` libero, `H` orizzontale, `V` verticale, `L` lunghezza, `G` angolo.

## Convenzioni di codice

- Stile Qt: `QVector`, `QVector3D`, `QStringLiteral`, `qBound`/`qMax`. Membri con suffisso `_`. Indentazione a 4 spazi. Graffa di apertura sulla stessa riga.
- Le enum sono spesso passate come `int` (display mode, plane, constraint mode, operazione booleana: `0` unione, `1` intersezione, `2` differenza). Se ne aggiungi una, mantieni la numerazione.
- Qualità di tessellazione `0/1/2` (bassa/media/alta) → densità dei campioni di display di curve e corpi (deflessione relativa alla diagonale del corpo).
- Il codice nuovo di geometria e algoritmi va in moduli separati `cad_*.h/.cpp` (namespace `ForgeCad`) e va aggiunto in `add_executable` in `CMakeLists.txt`. `forgeCad2026_gui.cpp` è già molto grande.

## Limiti e punti deboli noti

- OpenGL a pipeline fissa (compatibility profile). Il passaggio a shader/VBO sarebbe un refactor importante.
- Il backend CUDA non fa ancora calcoli: esegue solo un kernel vuoto di prova.

- La selezione dei corpi (`intersectRay`) ricostruisce un `IntCurvesFace_ShapeIntersector` a ogni movimento del mouse: con molti corpi o facce complesse andrà messo in cache.
- Il risolutore dei vincoli e' denso (matrici m x n): va bene per schizzi fino a qualche centinaio di punti. Non segnala i gradi di liberta' residui ne' i vincoli ridondanti (solo quelli in conflitto). Le spline e le NURBS accettano solo il vincolo fisso (e i loro punti tutti i vincoli di punto); le ellissi non si tagliano.
- Le booleane si riferiscono agli operandi per indice: l'eliminazione rinumera i riferimenti (`removeBodies`).